import { useRef, useState, useEffect } from "react";
import { faCheck, faTimes, faInfoCircle } from "@fortawesome/free-solid-svg-icons";  // Single import for icons
import { Link, Routes, Route, useMatch, useResolvedPath, useNavigate } from "react-router-dom";  // Combine all react-router-dom imports into one line
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";  // FontAwesomeIcon import remains the same

const manageTruck = () => {
};

export default manageTruck;
